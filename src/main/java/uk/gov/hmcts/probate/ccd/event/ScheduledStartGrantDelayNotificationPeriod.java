package uk.gov.hmcts.probate.ccd.event;

import org.springframework.stereotype.Component;
import uk.gov.hmcts.ccd.sdk.api.CCDConfig;
import uk.gov.hmcts.ccd.sdk.api.ConfigBuilder;
import uk.gov.hmcts.ccd.sdk.api.Permission;
import uk.gov.hmcts.probate.model.ccd.raw.request.CaseData;
import uk.gov.hmcts.probate.model.ccd.raw.request.State;
import uk.gov.hmcts.probate.model.ccd.raw.request.UserRole;

/**
 * Generated by ccd-definition-converter from GrantOfRepresentation on migration; owned by this service team.
 *
 * <p>Event {@code scheduledStartGrantDelayNotificationPeriod} for case type {@code GrantOfRepresentation}.
 */
@Component
public class ScheduledStartGrantDelayNotificationPeriod implements CCDConfig<CaseData, State, UserRole> {
    /**
     * The CCD event ID.
     */
    public static final String SCHEDULED_START_GRANT_DELAY_NOTIFICATION_PERIOD = "scheduledStartGrantDelayNotificationPeriod";

    /**
     * Registers event {@code scheduledStartGrantDelayNotificationPeriod}.
     *
     * @param builder the config builder
     */
    @Override
    public void configure(ConfigBuilder<CaseData, State, UserRole> builder) {
        builder.event(SCHEDULED_START_GRANT_DELAY_NOTIFICATION_PERIOD)
            .forAllStates()
            .name("Start grant delay period")
            .description("Start grant delay notification period")
            .displayOrder(147)
            .showSummary(false)
            .showEventNotes()
            .explicitGrants()
            .grant(Permission.CRUD, UserRole.CASEWORKER_PROBATE_SCHEDULER);
    }
}
