package uk.gov.hmcts.probate.ccd.event;

import java.util.EnumSet;
import org.springframework.stereotype.Component;
import uk.gov.hmcts.ccd.sdk.api.CCDConfig;
import uk.gov.hmcts.ccd.sdk.api.ConfigBuilder;
import uk.gov.hmcts.ccd.sdk.api.Permission;
import uk.gov.hmcts.probate.model.ccd.raw.request.CaseData;
import uk.gov.hmcts.probate.model.ccd.raw.request.State;
import uk.gov.hmcts.probate.model.ccd.raw.request.UserRole;

/**
 * Generated by ccd-definition-converter from GrantOfRepresentation on migration; owned by this service team.
 *
 * <p>Event {@code disposeCase} for case type {@code GrantOfRepresentation}.
 */
@Component
public class DisposeCase implements CCDConfig<CaseData, State, UserRole> {
    /**
     * The CCD event ID.
     */
    public static final String DISPOSE_CASE = "disposeCase";

    /**
     * Registers event {@code disposeCase}.
     *
     * @param builder the config builder
     */
    @Override
    public void configure(ConfigBuilder<CaseData, State, UserRole> builder) {
        builder.event(DISPOSE_CASE)
            .forStateTransition(EnumSet.of(State.Pending, State.SolAdmonCreated, State.SolAppCreatedDeceasedDtls, State.SolAppCreatedSolicitorDtls, State.SolAppUpdated, State.SolProbateCreated, State.SolIntestacyCreated, State.Deleted, State.Stopped), State.Disposed)
            .name("Dispose inactive draft")
            .displayOrder(197)
            .showSummary(false)
            .showEventNotes()
            .ttlIncrement(14)
            .explicitGrants()
            .grant(Permission.CRUD, UserRole.CASEWORKER_PROBATE_SCHEDULER, UserRole.CASEWORKER_PROBATE_SYSTEMUPDATE);
    }
}
